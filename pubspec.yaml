name: quicksplitz
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  provider: 6.1.4
  shared_preferences: ^2.2.2
  image_picker: ^1.1.2
  camera: ^0.9.4+3
  permission_handler: ^10.2.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo/logo.png
    - assets/images/qr/qr.png
